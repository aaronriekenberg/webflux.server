command-config:
  commands:

  - id: ifconfig
    description: ifconfig
    command: /sbin/ifconfig

  - id: ls
    description: ls
    command: /bin/ls
    arguments: [-l]

  - id: sleep
    description: sleep 1
    command: /bin/sleep
    arguments: [1]

proxy-config:
  proxies:

  - id: test_proxy
    description: google proxy
    url: http://www.google.com

static-paths:
  paths:
  - path: /vnstat.html

spring:
  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/webapp/,classpath:/public/,file:/Users/aaron/test/

---

spring:
  profiles: apu2
  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/webapp/,classpath:/public/,file:/home/aaron/vnstat/

command-config:
  commands:

  - id: ifconfig
    description: ifconfig
    command: /sbin/ifconfig

  - id: ls
    description: ls
    command: /bin/ls
    arguments: [-l]

proxy-config:
  proxies:
  - id: coredns_metrics
    description: coredns metrics
    url: http://localhost:9153/metrics

static-paths:
  paths:
  - path: /vnstat.html
